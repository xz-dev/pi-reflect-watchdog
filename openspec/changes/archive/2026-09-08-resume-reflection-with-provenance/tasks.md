## 1. Separate reflection content from the existing correction wake

- [x] 1.1 Add a failing provider-boundary regression to the existing runtime/stock-Pi fixtures for automatic and manual `ROUTE_CORRECTION`: verify one ordinary wake, the unchanged report under `assistant` for automatic reflection or `user` for manual `/reflect`, exactly `[assistant]\ncontinue` as the separate synthetic user body, and unchanged original user clarifications. Capture the expected failure before implementation; UI labels or `customType` alone are not the oracle.
- [x] 1.2 Add the bounded context projection around the existing inquiry fold, using a validated new marker, recorded manual/automatic trigger origin, and retained correlated source assistant. Project automatic reports with a real assistant envelope and manual reports as text-only user messages. Verify focused tests cover both roles independent of verdict, valid correlation, one report per marker, repeated context requests, multiple/reused inquiry IDs, preserved message ordering, and ordinary user text quoting the marker or `/reflect` without being reclassified or changing trigger origin.
- [x] 1.3 Replace the correction path's report-bearing custom content with one native wake carrying the existing report text, recorded trigger origin, and correlation only in private metadata; connect it to the trigger-specific projection without tagging the wake as an internal inquiry. Verify task 1.1 becomes green, `formatReflectionReport`/XML output remain unchanged, and any TUI rendering uses metadata without adding another message or turn.

## 2. Return control once for both valid verdicts

- [x] 2.1 Add the no-issue acceptance cases for automatic, busy-manual, and idle-manual reflection, alongside equivalent correction cases; assert automatic report delivery as `assistant` and both manual paths as `user`. Verify the pre-fix no-issue cases fail specifically because no ordinary continuation is scheduled, including a case where `next_step` says to wait rather than perform work.
- [x] 2.2 Route both valid result types through the same handoff while retaining the no-issue notice and caller-owned final dispatch. Verify all task 2.1 cases pass with exactly one plugin wake, the trigger-correct report role for either verdict, ordinary successful-turn accounting, no additional XML inquiry, and no task-completion inference from report text.
- [x] 2.3 Protect finalization ordering with focused runtime tests and a packed-host regression: verify internal state is cleared before reentrant message/context handling, projection can use the marker's report and trigger origin without a later result-entry lookup, completion evidence precedes queued reflection redispatch, and repeated settled events do not duplicate the handoff, stored completion, or hook.
- [x] 2.4 Update the stock-Pi no-issue request-count expectation for the new continuation without deleting its latched-threshold/cooldown assertion. Verify the extra request is ordinary work, cooldown blocks immediate automatic reflection, and a later reflection still follows the unchanged accounting policy.
- [x] 2.5 Keep terminal/error-path regressions intact. Verify invalid XML still has at most three attempts with one shared ten-tool-call budget; cancellation, missing decision, ownership loss, shutdown, and persistence failure do not acquire new replay/recovery behavior; completion hooks still require persisted result plus marker and retain their payload and optional-listener isolation.

## 3. Verify retention and actual provider representations

- [x] 3.1 Add reload/resume coverage using serializable new marker metadata, trigger origin, and retained source assistants. Verify each report is restored once with its original trigger-specific role (`assistant` for automatic, `user` for manual) without rescheduling a wake, including mixed-mode retained handoffs. Malformed/missing correlation or trigger origin must produce no reconstructed report or guessed role; old version-1 reports and unrelated extensions' messages remain untouched.
- [x] 3.2 Exercise Pi's normal compaction/branch-summary conversion separately from ordinary context projection. Verify a new conversational handoff contributes only the fixed wake in both trigger modes, report metadata is absent from serialized user text even for manual results, and removal of the source assistant prevents later reconstruction; do not assert report retention in the generated summary.
- [x] 3.3 Check the normalized provider messages and the available existing OpenAI-, Anthropic-, and Google-style request serializers offline for both trigger modes, including adjacent assistant content, adjacent manual user-role report/wake messages, a subsequent tool-loop request, and rewritten assistant response metadata. Verify each report remains unchanged and present once under assistant-equivalent roles for automatic reflection or user role for manual reflection; the exact wake remains distinct, including native content-block checks where serializers merge same-role messages. Record exact coverage and stop for review if a supported adapter cannot meet the contract without a scope change. Do not add dependencies or make live/paid provider calls.

## 4. Align documentation and verify the integrated change

- [x] 4.1 Update `README.md` to explain unified ordinary continuation, unchanged report text, automatic assistant-role versus manual user-role report delivery, the short wake's synthetic user role, and the retained-source/compaction limit in both modes. Verify it no longer describes `NO_ISSUE` as intentionally passive, imposes a blanket assistant role on manual reports, or claims that a text marker changes transport authorship.
- [x] 4.2 Update `docs/programming-thinking/pi-reflect-watchdog-lifecycle.idea.lean` so both valid verdicts queue one ordinary continuation while existing lifecycle, counter, cooldown, and cleanup invariants remain represented. Verify `lean docs/programming-thinking/pi-reflect-watchdog-lifecycle.idea.lean` and `lean --run docs/programming-thinking/pi-reflect-watchdog-lifecycle.idea.lean` succeed, using the existing toolchain; do not silently install a system toolchain if unavailable.
- [x] 4.3 Run `npm run check`, `npm run test:e2e:fast`, and `npm run test:e2e` using the existing runner. Verify all applicable suites pass and inspect the diff for unapproved changes to core/dependencies, report/XML/hook schemas, options, or compaction. Record an environment blocker rather than silently changing the execution protocol or installing persistent tools.
- [x] 4.4 Review implementation against this change's scenarios and run `openspec validate resume-reflection-with-provenance --strict`. Deliver a concise evidence summary with automatic/manual role coverage, provider/restore/compaction coverage, and any unverified real-model behavior; verify no task is marked complete solely from artifact existence or a fake-provider response. Leave publishing, deployment, archival, and live-model validation to separate authorization.
