## 1. Immediate manual steering

- [x] 1.1 Review the delta's busy-response, multi-tool, idle, and cancellation examples with the user before implementation; verify the delivery boundary and reduced cancellation window are explicitly accepted rather than inferred from green tests.
- [x] 1.2 Replace the busy-until-settle runtime expectation with an immediate manual-submission regression in `test/runtime.test.ts`; verify it fails on the current implementation because no inquiry is sent while busy, and asserts exactly one native `steer` submission with the original supplement and manual origin before any settlement event.
- [x] 1.3 Remove the manual-only ordinary-busy dispatch barrier in `src/extension.ts` while retaining shared ownership/context/outstanding-inquiry guards; verify the new regression passes and existing idle, automatic, child-busy, native-pending-message, pause/cooldown-bypass, and provisional-correlation checks remain correct.

## 2. Preserve serialization and honest cancellation

- [x] 2.1 Adapt existing queue, duplicate, cancel-command, configurable-shortcut, and status tests to create one plugin-pending request behind an outstanding inquiry; verify only that request can be cancelled, first-supplement coalescing remains intact, and native-queued/executing inquiries are neither retracted nor advertised as cancellable. Retain teardown coverage for discarded pending requests.
- [x] 2.2 Exercise finalization with a waiting manual request and an ordinary continuation that becomes busy; verify the waiting inquiry is submitted once after completion evidence is available, repeated settlement does not duplicate it, and report origin/role, XML re-asks, and one-continuation-per-valid-result behavior remain unchanged.

## 3. Native-boundary and documentation validation

- [x] 3.1 Add one focused regression to the existing stock-Pi integration/e2e fixtures using the loopback provider and controlled tools, without a new framework or external model; verify a manual request submitted during a multi-tool ordinary turn reaches the next eligible model request after all tool results and before ordinary-run settlement, with no abort or skipped tool. Record the tested Pi version; report a compatibility blocker rather than patching Pi or weakening the boundary if the target runtime differs.
- [x] 3.2 Update `README.md` to distinguish immediate native submission, later model consumption, and the plugin-only cancellation window; reconcile the affected existing lifecycle Lean model's dispatch/outstanding-inquiry scope without creating a second model. Verify documentation agrees with the delta, the exact model typechecks and runs with `lean` and `lean --run`, and an independent fresh-context semantic check confirms the affected modeled claims or reports an explicit validation blocker. (Semantic round-trip: sha256 f340df5799d5217db148467336b619d830bc840ac3d47c2102c2ccbcd07fb42c confirmed; reviewer correctly recovered queueManualReflection/dispatchReflection/reflectionFinished semantics.)
- [x] 3.3 Run `npm run check`, `npm run test:e2e:fast`, and the targeted stock-Pi boundary test, then obtain a focused independent review of dispatch/correlation/cancellation; verify all in-scope checks pass and present evidence for user acceptance. Keep unrelated aggregate-activity validation out of this change and do not claim source inspection proves live behavior. (Independent review completed: 1 valid Lean-model blocker fixed — dispatch now consumes the pending head, negative theorem single_manual_not_redispatched_after_finish added and green; 1 valid non-blocker fixed — tests split into consumed-completion and unconsumed-fold paths; 4 concerns disproven with code/test evidence. Post-fix validation: lean + lean --run green, axioms [propext], unit 57/57, biome+typecheck clean, e2e fast 30/30.)

## 4. Reconcile specifications after acceptance

- [x] 4.1 After implementation acceptance and authorization to sync, apply this delta to `manual-reflect-queue` and update the main spec's existing purpose so it no longer promises a cancellable wait throughout ordinary-agent busyness; verify the resulting requirements and purpose agree and OpenSpec strict validation passes. Leave archive/publication for a separately requested workflow.
